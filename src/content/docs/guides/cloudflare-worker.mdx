---
title: Example Guide
description: A guide in my new Starlight docs site.
---

```ts title="Example using Cloudflare cxt.env"
import { Invsy } from 'invsy'

export const invsy = (env: Env, userId: string) => {
    return new Invsy(
        env.INVSY_API_KEY,
        env.INVSY_PROJECT_ID,
        userId
    )
}
```
